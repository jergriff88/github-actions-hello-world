name: Daily Scheduled Workflow

on:
  schedule:
    - cron: '0 9 * * *'

jobs:
  daily-job:
    runs-on: ubuntu-latest

    steps:
      - name: Show Date
        run: date

      - name: Daily Message
        run: echo "This workflow runs every day at 9 AM UTC"
        